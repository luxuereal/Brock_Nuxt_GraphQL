mutation ForgotPassword($email: String!, $resetPasswordUrl: String!) {
  forgotPassword(
    forgotPasswordInput: { email: $email, resetPasswordUrl: $resetPasswordUrl }
  ) {
    status
    message
  }
}
